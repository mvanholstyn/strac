<%= error_messages_for :story %>

<% remote_form_for(:story, :url => story_path(@project, @story), :html => { :method => :put }) do |form| -%>
  <%= render :partial => 'form', :locals => { :form => form } %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end -%>