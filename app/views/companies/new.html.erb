<h1>New company</h1>

<%= error_messages_for :company %>

<% form_for(:company, :url => companies_path) do |f| %>
  <p>
    <b>Name</b><br />
    <%= f.text_field :name %>
  </p>

  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', companies_path %>