
<div class="iteration">
	<div class="header-stats">
	  <span class="stat">Budgeted: <%= h @iteration.budget %></span>
	  <span class="stat">Completed: <%= h @iteration.points_completed %></span>
	  <span class="stat">Remaining: <%= h @iteration.points_remaining %></span>
	</div>
	<div class="header">
	<b>Iteration:</b> <%=h @iteration.name %><br /><br />
	</div>
</div>

Stories<br/>
<hr />
<%= render :partial => 'stories/list', :locals => { :stories => @iteration.stories } %>