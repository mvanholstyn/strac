<table width="100%">
  <tr class="iteration">
    <td>
			<%= render :partial => 'iterations/header', :locals => { :iteration => iteration } %>
    </td>
    <td align="right" class="actions">
      <%= link_to_remote "create story", 
        :url => new_story_path(:project_id => iteration.project.id, :bucket_id => iteration.id ), 
        :method => :get,
        :html => { :class => "create-story" } %></td>
  </tr>
</table>
<%= render :partial => "iterations/stories", :locals => {:iteration=>iteration, :stories=>iteration.stories} %>
<br />
